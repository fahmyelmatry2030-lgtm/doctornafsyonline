name: Disabled - Manual Deploy Only

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual deploy'
        required: false

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Manual deployment only. Please deploy via Hostinger hPanel."
